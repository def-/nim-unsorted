proc comb(m, s): seq[seq[int]] =
  if m == 0:
    result = @[]
    result.add(@[])
  else:
    result = @[]
    for i, x in s:
      var n = @[x]
      for 
      result.add(@[x])

comb(3, @[0,1,2,3,4])
